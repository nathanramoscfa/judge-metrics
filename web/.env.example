# web/.env.example
# Copy to `web/.env.local` (untracked). Only NEXT_PUBLIC_* variables reach
# the client bundle; the API base URL is public by design. Server components
# fetch from the same URL on the server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
