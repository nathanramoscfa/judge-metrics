# src/judgemetrics/ingest/fjc/__init__.py
"""The Federal Judicial Center connector (docs/DATA_SOURCES.md, ``fjc``).

``sources`` holds the verified URLs, ``schema`` the verified headers and
vocabularies, ``parse`` the Polars CSV reader, ``normalize`` the mapping
from source rows to canonical drafts, and ``connector`` the registered
``FjcConnector``.
"""
